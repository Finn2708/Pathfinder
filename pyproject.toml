[project]
name = "pathfinder"
version = "0.1.0"
description = "Yet another 2D visualization tool for different path finding algorithms"
requires-python = ">=3.12"
dependencies = [
    "pygame>=2.6.1",
]
